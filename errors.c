/*#include "so_long.h"
#include <stdlib.h>

void	print_error_exit(char *error, char *name, t_game *game)
{
	ft_putstr_fd("Error\n", 2);
	ft_putstr_fd(error, 2);
	if (name)
		ft_putstr_fd(name, 2);
	ft_putstr_fd("\n", 2);
	if (game)
		free_game(game);
	exit(EXIT_FAILURE);
}*/
//da vedere
